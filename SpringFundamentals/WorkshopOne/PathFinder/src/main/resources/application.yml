logging:
    level:
        org:
            hibernate:
                SQL: DEBUG
                type:
                    descriptor: TRACE
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: test
        url: jdbc:mysql://localhost:3306/pathfinder?createDatabaseIfNotExist=true
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
                format_sql: true
